version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    environment:
    volumes:
        

  augie_cs_discord_bot:
    image: noah-witt/augie_cs_discord_bot
    restart: always
    ports:
      - 8081:8081
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: root
      ME_CONFIG_MONGODB_ADMINPASSWORD: example