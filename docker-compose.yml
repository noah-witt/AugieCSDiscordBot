version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    environment:
    volumes:
        

  augie_cs_discord_bot:
    build: .
    restart: always
    ports:
      - localhost:8080:8080
    environment:
      MONGO: mongodb://db:27017/results2020
      discordLogin: <FILL IN DISCORD LOGIN>
      discordChannelId: <FILL IN CHANNEL ID>
      TZ: America/Chicago
      enableWeb: true
    links:
      - "mongo:db"